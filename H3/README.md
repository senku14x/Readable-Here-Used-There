# H3 — downstream use of readable representations

Two planning documents, both context rather than registered designs:


A registered design for whichever branch runs goes beside them as `design_specs/<experiment>.md` before any forward, per the project's lifecycle.

**What has run (index, 2026-09-24).** Design specs in `design_specs/`, batteries and analyses in `scripts/`, raw arrays and manifests in `outputs/`, reports in `results/reports/`, tables in `results/tables/`, figures in `results/figures/`. Experiments: `selection_to_behavior` (+ `carrier_only`), `computed_sum_consumer` (+ `robust`, `global`), `query_local_workspace` (+ `clampfix`, `clampfix32`), `two_hop_organism` (pilot, full, no-clue, `bridgeswap`, `qsplit` Amendment 5, `qsplit6` Amendment 6, Stage 2 `admit`/`stage2` Amendment 7; Amendment 8 registered, not run), `recompute_cost_ladder`, `consumer_competence`, `trying_to_fool`. The canonical H3 statement is the block at the top of `STATUS.md`; the claims table with withdrawals is `context/evidence_ledger.md` §E.
