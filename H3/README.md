# H3 — downstream use of readable representations

Two planning documents, both context rather than registered designs:


A registered design for whichever branch runs goes beside them as `design_specs/<experiment>.md` before any forward, per the project's lifecycle. Nothing in H3 has been run.
